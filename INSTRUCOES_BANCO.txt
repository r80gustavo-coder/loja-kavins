-- RODE ESTE COMANDO NO "SQL EDITOR" DO SEU SUPABASE PARA CRIAR O BANCO --

create table products (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  name text,
  category text,
  composition text,
  tech_composition text,
  images jsonb, 
  variants jsonb
);

-- IMPORTANTE: Depois de rodar isso, vá em "Authentication > Policies" 
-- e desative o RLS (Row Level Security) ou crie uma politica "Enable all" 
-- para que o site consiga salvar sem login complexo.
